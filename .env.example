# This file is an example. Create a .env file with your actual credentials.

# Database
DATABASE_URL="postgresql://user:password@host:port/db"

# Authentication
JWT_SECRET="generate-strong-secret-here-for-jwt"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-strong-secret-here-for-nextauth"

# Email Provider (resend or mailgun)
EMAIL_PROVIDER="resend"
RESEND_API_KEY="re_xxxxx"
MAILGUN_API_KEY="key-xxxxx"
MAILGUN_DOMAIN="mg.yourdomain.com"
EMAIL_FROM="noreply@yourdomain.com"

# Firebase - for production, use GOOGLE_APPLICATION_CREDENTIALS_JSON
FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
# In production, set this to the JSON content of your service account file.
# It should be a single line string.
GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account", "project_id": "...", ...}'

# Demo Mode - Enable to use in-memory database with persistent state
# Set to "true" or "1" to enable demo mode (no external DB connections)
# In demo mode: No PostgreSQL, Firebase, or email service connections
# Demo login: admin@hestiaplp.com.mx / password123
DEMO_MODE="false"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
