/**
 * @deprecated Relocated to the domain layer with S5a (#133). Import from
 * `@/lib/domain/policy/adapters/form` — this shim exists only until the six
 * wizard step components migrate their imports.
 */
export * from '@/lib/domain/policy/adapters/form';
